<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="LOG_HOME" value="/Users/mapeichuan/gejia/auto-earn"/>
    <include resource="org/springframework/boot/logging/logback/defaults.xml" />
    <include resource="org/springframework/boot/logging/logback/console-appender.xml" />
    <property name="FILE_LOG_PATTERN" value="[%d{yyyy-MM-dd HH:mm:ss}] [%level] [%X{traceId}] [%-22thread]  %logger{35}.%-5L - %msg %n"/>


    <!--<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    &lt;!&ndash; info日志 appender  &ndash;&gt;
    <appender name="TEST" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <File>${LOG_HOME}/test.log</File>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <FileNamePattern>${LOG_HOME}/test.%d{yyyy-MM-dd}.log.gz</FileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <Pattern>${FILE_LOG_PATTERN}</Pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">&lt;!&ndash; 只打印INFO日志 &ndash;&gt;
            <level>ALL</level>
&lt;!&ndash;            <onMatch>ACCEPT</onMatch>&ndash;&gt;
&lt;!&ndash;            <onMismatch>DENY</onMismatch>&ndash;&gt;
        </filter>
    </appender>


    <logger name="test-log" level="INFO" additivity="true">
        <appender-ref ref="TEST"/>
        <appender-ref ref="STDOUT"/>
    </logger>-->


</configuration>